/**
 * JNIX's Scheduler
 * 
 * In charge of allocating threads to processes and such.
 */

export class Scheduler {

    /**
     * The running thread pool.
     * @type { Map<number, *> }
     **/
    #threads = new Map();

    
}